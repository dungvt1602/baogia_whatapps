import TemplatesScreen from "@/features/templates/TemplatesScreen";

// Chi tiết template — id lấy từ URL /template/[id] (TemplatesScreen đọc pathname).
export default function Page() {
  return <TemplatesScreen />;
}
